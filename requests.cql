// Requête 1
match ()-[:ACTED_]-

// Requête 2
match ()-[:ACTED_]-

// Requête 3
match ()-[:ACTED_]-

// Requête 4
match ()-[:ACTED_]-

// Requête 5
match ()-[:ACTED_]-

// Requête 6
match ()-[:ACTED_]-

// Requête 7
match ()-[:ACTED_]-

// Requête 8
match ()-[:ACTED_]-

// Requête 9
match ()-[:ACTED_]-
